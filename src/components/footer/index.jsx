import React from "react";

const Footer = ({ title }) => {
  return (
    <footer className="bg-blue-600 text-white py-3 text-center mt-auto">
      <p>{title}</p>
    </footer>
  );
};

export default Footer;
